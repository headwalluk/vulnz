# .env.wordfence (sample)
# Copy to .env.wordfence and edit the values as needed.

# VULNZ API base (include /api)
VULNZ_API_URL=http://localhost:3000/api

# An API key with permissions to add component releases and vulnerabilities
VULNZ_API_KEY=YOUR_API_KEY_HERE

# Optional: persistent working directory used for cache/state
# Recommended: avoids re-downloading and preserves processed IDs
WORK_DIR=/var/local/vulnz

# Optional: enable extra logging from the script
ENABLE_DIAGNOSTICS=false

# Optional: limit the number of Wordfence entries processed per run
# If unset, the script defaults to 20
BATCH_SIZE=20
